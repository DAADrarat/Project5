<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-cal">
	<insert id="insert" parameterType="lx.project.calander.to.MemberTO">
	
		insert into addrbook(ab_name, ab_email, ab_comdept, ab_birth, ab_tel, ab_memo)
		values(#{abName}, #{abEmail}, #{abComdept}, #{abBirth}, #{abTel}, #{abMemo})
	</insert>
	
	
	<update id="update" parameterType="lx.project.calander.to.MemberTO">
				update addrbook 
				<trim prefix="SET" suffixOverrides=",">
					<if test="abName != null">ab_name = #{abName}, </if>
					<if test="abEmail != null">ab_email = #{abEmail}, </if>
					<if test="abComdept != null">ab_Comdept = #{abComdept}, </if>
					<if test="abBirth != null">ab_Birth = #{abBirth}, </if>
					<if test="abTel != null">ab_Tel = #{abTel}, </if>
					<if test="abMemo != null">ab_Memo = #{abMemo} </if>
				</trim>
				where ab_id = #{abId}
	</update>
	
	<select id="getlist" resultType="lx.project.calander.to.MemberTO">
		select ab_id
			,ab_name
			,ab_email
			,ab_comdept
			,ab_birth
			,ab_memo
			,ab_tel
		from addrbook
	</select>
	
	<select id="getById" parameterType="int" resultType="lx.project.calander.to.MemberTO">
		select ab_id
			,ab_name
			,ab_email
			,ab_comdept
			,ab_birth
			,ab_memo
			,ab_tel
		from addrbook
		where ab_id = #{abId}
	</select>
	
	
	<delete id="delete" parameterType="lx.project.calander.to.MemberTO">
		delete from addrbook
		where ab_id = #{abId}
	</delete>
	
	
	
</mapper>